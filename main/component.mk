#
# Main component makefile.
#
# This Makefile can be left empty. By default, it will take the sources in the 
# src/ directory, compile them and link them into lib(subdirectory_name).a

# Makefile này có thể để trống. Theo mặc định, nó sẽ lấy các nguồn trong
# src / directory, biên dịch chúng và liên kết chúng thành lib (subdirectory_name) .a

# in the build directory. This behaviour is entirely configurable,
# please read the ESP-IDF documents if you need to do this.

# trong thư mục xây dựng. Hành vi này hoàn toàn có thể định cấu hình,
# vui lòng đọc tài liệu ESP-IDF nếu bạn cần thực hiện việc này.
